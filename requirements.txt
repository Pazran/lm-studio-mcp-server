# lm-studio-mcp-server dependencies
# Core MCP Server
mcp>=1.0.0

# Data Processing
pandas>=2.0.0

# PDF Reading (for read_pdf_text tool)
pypdf>=4.0.0

# File Operations (safe delete to Recycle Bin)
send2trash>=1.8.0

# Web Search via SearXNG
requests>=2.31.0

# HTML Parsing (web search results)
beautifulsoup4>=4.12.0

# Optional: Git integration (if using git_mcp_server.py)
gitpython>=3.1.0

# Development/Testing (optional)
pytest>=7.0.0
